{
  "name": "solar-system-glm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Educational 3D solar system explorer with spaceship navigation and an LLM knowledge pipeline.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "@types/three": "^0.170.0",
    "typescript": "~5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
